<!DOCTYPE html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <%=favicon_link_tag 'qa.jpg', sizes: '32x32'%>
  <title>QA</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet"/>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
  <style>
      #overlay {
          background: white;
          color: black;
          position: fixed;
          height: 100%;
          width: 100%;
          z-index: 5000;
          top: 0;
          left: 0%;
          float: left;
          text-align: center;
          padding-top: 25%;
          font-size: 20px;
          font-family: monaco,Consolas,Lucida Console,monospace;
      }

      .loader1 {
          z-index: 152;
          position: fixed;
          left: 54%;
          margin-top: -2em;
          display: inline-flex;
          flex-wrap: wrap;
          width: 90px;
          height: 90px;
          transform-style: preserve-3d;
          transform: rotateX(45deg) rotate(45deg);
      }

  </style>
  <div id="overlay">
    <%= t :loader %>
    <div class="loader1" alt="Loading" >
      <div class="box"></div>
      <div class="box"></div>
    </div>
  </div>
  <script>
      $(window).on("load", function(){
          $('#overlay').fadeOut();
          setTimeout(() => {
              console.log("Loading page");

          }, 2500);
      });
  </script>

  <script>
      window.App = {
          config: {
              ip: '<%= request.ip %>'
          }
      };

      function show_hide(params){
          if ($("[href=\"#windows10\"]").attr("aria-expanded") === `${params.win}`) {
              let icon = document.querySelector("#icon-windows")
              icon.click()
          }
          if ($("[href=\"#linux\"]").attr("aria-expanded") === `${params.linux}`) {
              let icon = document.querySelector("#icon-linux")
              icon.click()
          }
          if ($("[href=\"#mac\"]").attr("aria-expanded") === `${params.mac}`) {
              let icon = document.querySelector("#icon-mac")
              icon.click()
          }
          if ($("[href=\"#fedora\"]").attr("aria-expanded") === `${params.fedora}`) {
              let icon = document.querySelector("#icon-fedora")
              icon.click()
          }

          if ($("[href=\"#back\"]").attr("aria-expanded") === `${params.back}`) {
              let icon = document.querySelector("#icon-back")
              icon.click()
          }
          if ($("[href=\"#mobile\"]").attr("aria-expanded") === `${params.mobile}`) {
              let icon = document.querySelector("#icon-mobile")
              icon.click()
          }
          if ($("[href=\"#gui\"]").attr("aria-expanded") === `${params.gui}`) {
              let icon = document.querySelector("#icon-web")
              icon.click()
          }
      }
  </script>

  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= render 'shared/spinner'%>

</head>
<%= yield %>
